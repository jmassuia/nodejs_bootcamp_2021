hearder.header
    nav.nav.nav--tours
        a.nav__el(href="/overview") All Tours
    .header__Logo
        img(src="/img/logo-white.png" alt="Natours logo") 
    nav.nav.nav--users
        if user
            a.nav__el.nav__el--logout(href="#") Log out
            a.nav__el(href="#")
            img(src=`/img/users/${user.photo}` alt=`${user.name}` class="nav__user-img")
            span= user.name.split(' ')[0]
        else
            a.nav__el(href='/login') Log in
            a.nav_el.nav__el--cta Sign Up